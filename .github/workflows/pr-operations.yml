name: Release Open Source Airbyte
on:
  workflow_dispatch:
    inputs:
      partToBump:
        description: "Please choose the type of version upgrade : major|minor|patch"
        required: true
        default: "patch"
jobs:
  releaseAirbyte:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Release Airbyte
        id: release_airbyte
        env:
          PART_TO_BUMP: ${{ github.event.inputs.partToBump }}
        run: |
          ./bump_version.sh
      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.SUBODH_GITHUB_ACCESS_TOKEN }}
          branch: bump-version
          branch-suffix: random
          delete-branch: true
          title: Bump Airbyte version - ${{ github.event.inputs.partToBump }}
          body: Bump Airbyte version - ${{ github.event.inputs.partToBump }}
          commit-message: Bump Airbyte version - ${{ github.event.inputs.partToBump }}
      - name: PR Details
        run: |
          echo "Pull Request Number - ${{ steps.cpr.outputs.pull-request-number }}"
          echo "Pull Request URL - ${{ steps.cpr.outputs.pull-request-url }}"